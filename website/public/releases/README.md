# Update manifests (do not move)

Written by release.yml on every tagged release. Shipped apps read these from
sudovoice.com/releases/* (proxied by the private website repo) and from this
path directly as the raw-GitHub fallback. The website itself lives in the
private `sudovoice-cloud` repo (open-core split).
